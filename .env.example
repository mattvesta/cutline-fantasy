# Database
POSTGRES_PASSWORD=change_me

# Redis (optional override)
# REDIS_URL=redis://localhost:6379

# Anthropic (hosted platform only — not required for self-hosting)
# ANTHROPIC_API_KEY=

# Sports data (all defaults are free and require no API keys)
# ESPN_POLL_INTERVAL_SECONDS=60
